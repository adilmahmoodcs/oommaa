<h3>Role Rights</h3>
<div class='wrapper wrapper-content animated fadeInRight'>
  <div class="row">
      <div class="col-lg-12">
        <div class="ibox float-e-margins">
          <% Right.pluck('category').uniq.each do |category| %>
            <%= render 'rights_form_wrt_category', category: category %>
          <% end %>
        </div>
      </div>
  </div>
</div>
